'use strict';
import React from 'react';
//import Lightbox from 'react-images/src/Lightbox.js';
import Lightbox from 'react-images';

console.dir(Lightbox);
export default class Slideshow extends React {
  renderDialog() {
    console.log(1);
    if (result) {
      console.log(result);
    }
  }
}
